#include <bits/stdc++.h>
using namespace std;

int main() {
  int N,W;
  cin >> N >> W;
  priority_queue<int, vector<int>, less<int>> Q;
  for (int i = 0; i < N; i++) {
    int A,B;
    cin >> A >> B;
    for (int j = 0; j < B;)
  }
}