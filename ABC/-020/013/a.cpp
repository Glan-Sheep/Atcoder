#include <bits/stdc++.h>
using namespace std;

int main() {
  string X;
  cin >> X;
  if (X == "A") {
    cout << 1 << endl;
  }
  if (X == "B") {
    cout << 2 << endl;
  }
  if (X == "C") {
    cout << 3 << endl;
  }
  if (X == "D") {
    cout << 4 << endl;
  }
  if (X == "E") {
    cout << 5 << endl;
  }
}