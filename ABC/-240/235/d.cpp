#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main() {
  ll a, n;
  cin >> a >> n;
  int x = 1;
  while(true) {
    if (x >= 10 && x % 10 == 0) {
      string index = to_string(x);
      int indox = index.size();
      
    }
  }
}