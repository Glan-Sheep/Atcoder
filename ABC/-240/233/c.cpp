#include <bits/stdc++.h>
using namespace std;

int main() {
  int n;
  cin >> n;
  long long x;
  cin >> x;
  vector<vector<long long> > data(n, vector<long long>(n));
  vector<vector<bool> > date(n , vector<bool>(n,false));
  for (int i = 0; i < n; i++) {
    for (int j = 0; j < n; j++) {
      cin >> data[i][j];
    }
  }
  int ans = 0;
  while (true) {
    if ()
    for (int i = 0; i < n; i++) {
      int mas = 0;
      for (int j = 1; j < data[n][0]; j++) {
      }
    }
  }
}